# ai-analysis

Baseline project scaffold under the `hermes` workspace.

## Conventions
- Conventional Commits
- `main` as default branch
- Remote on GitHub
- `git worktree` used for parallel task branches under `../_worktrees/`
